---
phase: 16-end-to-end-validation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [tests/e2e-install.bats]
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]

must_haves:
  truths:
    - "Fresh GSD project install succeeds and produces all expected artifacts"
    - "Project with existing .claude/ files preserves pre-existing content during install"
    - "Non-GSD repo (no .planning/) causes installer to fail with actionable guidance"
    - "Install-then-dry-run chain produces valid ralph-launcher.sh output in installed repo"
    - "Re-running installer on an already-installed repo changes zero files"
    - "All tests execute in isolated temp directories with no side effects on dev repo"
  artifacts:
    - path: "tests/e2e-install.bats"
      provides: "End-to-end install workflow scenario tests"
      min_lines: 80
      contains: "e2e:"
  key_links:
    - from: "tests/e2e-install.bats"
      to: "install.sh"
      via: "bash $INSTALLER invocation in each test scenario"
      pattern: 'bash "\$INSTALLER"'
    - from: "tests/e2e-install.bats"
      to: "scripts/ralph/ralph-launcher.sh"
      via: "dry-run invocation of installed launcher"
      pattern: "scripts/ralph/ralph-launcher.sh execute-phase"
    - from: "tests/e2e-install.bats"
      to: "tests/test_helper/common.bash"
      via: "load test_helper/common for setup/teardown"
      pattern: "load.*test_helper/common"
---

<objective>
Create end-to-end test suite proving the complete install-then-use workflow works in realistic target repos with varying initial states.

Purpose: Phase 16 is the final validation gate for v2.1 Easy Install. It chains operations together (create repo, install, use installed artifacts) to prove the workflow functions as a whole, complementing the 32 unit/integration tests in installer.bats.

Output: tests/e2e-install.bats with scenario-driven tests covering all 4 success criteria.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-end-to-end-validation/16-RESEARCH.md

@install.sh
@tests/installer.bats
@tests/test_helper/common.bash
@tests/test_helper/ralph-helpers.bash
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD end-to-end install workflow scenarios</name>
  <files>tests/e2e-install.bats</files>
  <behavior>
    - Test "e2e: fresh GSD project -- install succeeds and all artifacts exist": create_test_repo + create_gsd_structure + create_ralph_config_raw, run installer, assert_success, verify scripts/ralph/ directory exists with 4 executable scripts, .claude/commands/gsd/ralph.md exists, .claude/skills/gsd-ralph-autopilot/SKILL.md exists, ralph key present in config.json via jq -e '.ralph'
    - Test "e2e: existing .claude/ files preserved during install": create pre-existing .claude/settings.local.json and .claude/commands/existing.md, run installer, assert pre-existing files survive with original content intact, assert ralph files also installed
    - Test "e2e: non-GSD repo -- installer fails with guidance": create_test_repo only (no GSD structure), run installer, assert_failure, assert_output --partial "GSD"
    - Test "e2e: installed ralph-launcher.sh produces valid dry-run output": create full GSD project with STATE.md having "Phase: 1" line, create .planning/phases/1-test-phase/1-01-PLAN.md with minimal frontmatter, run installer, then run bash scripts/ralph/ralph-launcher.sh execute-phase 1 --dry-run, assert_success, assert_output --partial "Ralph Dry Run", assert_output --partial "execute-phase"
    - Test "e2e: full re-install produces zero file changes (idempotent)": run installer once, record timestamps of key files via stat -f "%m", sleep 1, run installer again, assert_success, assert all timestamps unchanged, assert output --partial "Already up to date"
  </behavior>
  <action>
    **RED phase:** Create tests/e2e-install.bats with all 5 scenario tests described in the behavior block.

    Test file structure:
    ```
    setup() {
        load 'test_helper/common'
        load 'test_helper/ralph-helpers'
        _common_setup
        REAL_PROJECT_ROOT="$(get_real_project_root)"
        INSTALLER="$REAL_PROJECT_ROOT/install.sh"
    }
    teardown() { _common_teardown; }
    ```

    Each test is a self-contained scenario that creates its own temp environment using the existing helpers. Use create_test_repo + create_gsd_structure + create_ralph_config_raw with '{"mode":"yolo","parallelization":true}' (no ralph key -- installer adds it).

    For the dry-run test, create a minimal phase directory structure (.planning/phases/1-test-phase/1-01-PLAN.md with minimal frontmatter) and STATE.md with "Phase: 1" line.

    For timestamp tests, use stat -f "%m" (macOS BSD stat, consistent with installer.bats).

    Keep tests focused on workflow chains, not individual operations (that is installer.bats territory). Use assert_output --partial for all output matching (not exact). Target 5 scenario tests, under 150 lines.

    Run tests -- they MUST all fail (tests exercise install.sh which already exists and works, so the tests should actually pass immediately since this is a validation phase writing tests against an already-working installer).
    Commit: `test(16-01): add failing tests for e2e install workflow scenarios`

    **GREEN phase:** Since install.sh already exists and is fully functional from Phase 15, the tests should pass against the existing implementation. Run the tests. If any test fails, the issue is in the test setup (incorrect helper usage, wrong assertions, missing temp directory scaffolding), not in install.sh. Debug and fix the test code until all 5 scenarios pass. Do NOT modify install.sh or any existing source files -- only fix the test file itself.
    Commit: `test(16-01): green e2e install workflow scenarios`

    **REFACTOR (if needed):** Deduplicate any repeated setup patterns across tests (e.g., extract a helper function for "create full GSD project with phase structure"). Run tests again to confirm pass.
  </action>
  <verify>
    <automated>./tests/bats/bin/bats tests/e2e-install.bats</automated>
  </verify>
  <done>tests/e2e-install.bats exists with 5 passing scenario tests: fresh install creates all artifacts, existing .claude/ files preserved, non-GSD repo fails with guidance, installed launcher produces valid dry-run output, re-install is idempotent with zero file changes. Full test suite (351 existing + 5 new) passes with 0 failures.</done>
</task>

</tasks>

<verification>
./tests/bats/bin/bats tests/e2e-install.bats
./tests/bats/bin/bats tests/*.bats
</verification>

<success_criteria>
- All 5 E2E scenario tests pass (fresh install, existing .claude/, non-GSD, dry-run, idempotency)
- Full test suite green (351 existing + new E2E tests, 0 failures)
- tests/e2e-install.bats exists with scenario-driven tests using existing helpers only
- No modifications to existing test files or helpers
</success_criteria>

<output>
After completion, create `.planning/phases/16-end-to-end-validation/16-01-SUMMARY.md`
</output>
