---
phase: 11-shell-launcher-and-headless-invocation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/commands/gsd/ralph.md
  - scripts/ralph-launcher.sh
  - tests/ralph-launcher.bats
  - tests/ralph-permissions.bats
  - tests/test_helper/ralph-helpers.bash
autonomous: true
requirements: [AUTO-01, AUTO-05, PERM-01, PERM-02, PERM-03, SAFE-01, SAFE-02]

must_haves:
  truths:
    - "User can invoke /gsd:ralph execute-phase 11 and it triggers the launcher script"
    - "Running --dry-run shows the exact claude -p command without executing it"
    - "Default tier produces --allowedTools with the hardcoded whitelist"
    - "Auto-mode tier produces --permission-mode auto"
    - "Yolo tier produces --dangerously-skip-permissions"
    - "Every command includes --worktree flag"
    - "Every command includes --max-turns from config (default 50)"
  artifacts:
    - path: ".claude/commands/gsd/ralph.md"
      provides: "GSD command entry point for /gsd:ralph"
      contains: "$ARGUMENTS"
    - path: "scripts/ralph-launcher.sh"
      provides: "Launcher with arg parsing, config reading, permission mapping, command building, dry-run"
      min_lines: 100
      exports: ["parse_args", "read_config", "build_permission_flags", "build_prompt", "build_claude_command", "dry_run_output"]
    - path: "tests/ralph-launcher.bats"
      provides: "Tests for launcher arg parsing, command building, dry-run, worktree, max-turns"
      min_lines: 50
    - path: "tests/ralph-permissions.bats"
      provides: "Tests for permission tier flag mapping"
      min_lines: 30
  key_links:
    - from: ".claude/commands/gsd/ralph.md"
      to: "scripts/ralph-launcher.sh"
      via: "bash invocation instruction"
      pattern: "ralph-launcher.sh"
    - from: "scripts/ralph-launcher.sh"
      to: "scripts/validate-config.sh"
      via: "source"
      pattern: "source.*validate-config.sh"
    - from: "scripts/ralph-launcher.sh"
      to: "scripts/assemble-context.sh"
      via: "function call"
      pattern: "assemble-context.sh"
---

<objective>
Build the /gsd:ralph command file and the launcher script's core: argument parsing, config reading, permission tier mapping, claude -p command construction, worktree/max-turns flags, and --dry-run preview. This plan delivers everything except the actual iteration loop (Plan 02).

Purpose: The user needs a working --dry-run that shows the exact claude -p command, and all permission/safety flags must be correctly mapped. This is the foundation the loop engine builds on.
Output: /gsd:ralph command file, ralph-launcher.sh with core functions, full TDD test coverage for command building and permissions.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11-shell-launcher-and-headless-invocation/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from Phase 10 deliverables. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From scripts/assemble-context.sh:
```bash
# Usage:
#   assemble-context.sh              # Output to stdout
#   assemble-context.sh /path/to/out # Output to file
# Reads STATE.md + active phase plan files. Outputs combined context blob.
# Exit 1 if STATE.md not found.
```

From scripts/validate-config.sh:
```bash
# Usage: source this file and call validate_ralph_config <config_file>
# Returns 0 always (strict-with-warnings). Warnings on stderr.
validate_ralph_config() { ... }
```

From .planning/config.json:
```json
{
  "ralph": {
    "enabled": true,
    "max_turns": 50,
    "permission_tier": "default"
  }
}
```

From tests/test_helper/ralph-helpers.bash:
```bash
get_real_project_root()        # Returns real project root (not TEST_TEMP_DIR)
create_ralph_config()          # Args: [enabled] [max_turns] [permission_tier]
create_ralph_config_raw()      # Args: raw JSON string
create_mock_state()            # Creates STATE.md with Phase 10 of 12 content
```

From ARCHITECTURE.md boundaries:
- NEVER manage worktrees -- use Claude Code --worktree
- NEVER invoke GSD slash commands in headless mode -- use natural language prompts
- NEVER use --system-prompt (replaces defaults) -- use --append-system-prompt-file (adds to defaults)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD -- Test scaffolds and launcher core functions</name>
  <files>tests/ralph-launcher.bats, tests/ralph-permissions.bats, tests/test_helper/ralph-helpers.bash, scripts/ralph-launcher.sh</files>
  <behavior>
    RED (tests/ralph-permissions.bats):
    - Test: default tier produces "--allowedTools" with "Write,Read,Edit,Grep,Glob,Bash(*)"
    - Test: auto-mode tier produces "--permission-mode auto"
    - Test: yolo tier produces "--dangerously-skip-permissions"
    - Test: invalid tier returns error

    RED (tests/ralph-launcher.bats):
    - Test: parse_args extracts GSD command from arguments
    - Test: parse_args detects --dry-run flag
    - Test: parse_args extracts --tier override
    - Test: read_config reads max_turns from config.json
    - Test: read_config reads permission_tier from config.json
    - Test: read_config uses defaults when config missing
    - Test: build_prompt translates "execute-phase 11" into natural language prompt
    - Test: build_claude_command includes --worktree flag (SAFE-01)
    - Test: build_claude_command includes --max-turns from config (SAFE-02)
    - Test: build_claude_command includes --output-format json
    - Test: build_claude_command includes --append-system-prompt-file with context file path
    - Test: build_claude_command uses correct permission flags for each tier
    - Test: dry_run_output prints command without executing (AUTO-05)
    - Test: dry_run_output shows config summary (max_turns, permission_tier, worktree: always on)

    GREEN (scripts/ralph-launcher.sh):
    - Implement parse_args: parse GSD command, --dry-run, --tier from $@ arguments
    - Implement read_config: read ralph.max_turns and ralph.permission_tier from config.json via jq, with defaults (50, "default")
    - Implement build_permission_flags: case statement mapping tier to CLI flags
    - Implement build_prompt: translate GSD slash command to natural language for claude -p
    - Implement build_claude_command: assemble full claude -p command string with all flags
    - Implement dry_run_output: print command, context summary, config values
    - Source validate-config.sh for startup validation
    - All functions must be individually callable (not wrapped in main flow) for testability
    - Bash 3.2 compatible: no associative arrays, no ${var,,}, use date -u +%Y-%m-%dT%H:%M:%SZ

    ALSO update tests/test_helper/ralph-helpers.bash:
    - Add create_mock_claude_command helper that creates a mock `claude` executable in PATH for testing
    - Add create_context_file helper that creates a temp context file for testing
  </behavior>
  <action>
    Follow TDD RED-GREEN cycle:

    1. RED: Create tests/ralph-permissions.bats with 4 tests for build_permission_flags function. Create tests/ralph-launcher.bats with ~14 tests for parse_args, read_config, build_prompt, build_claude_command, dry_run_output. Update tests/test_helper/ralph-helpers.bash with new helpers. Run tests -- all must FAIL.

    2. GREEN: Create scripts/ralph-launcher.sh with all functions. The script structure:
       - Shebang: #!/bin/bash, set -euo pipefail
       - PROJECT_ROOT detection via git rev-parse --show-toplevel
       - Constants: DEFAULT_MAX_TURNS=50, DEFAULT_PERMISSION_TIER="default", DEFAULT_ALLOWED_TOOLS="Write,Read,Edit,Grep,Glob,Bash(*)"
       - source validate-config.sh for config validation
       - Functions: parse_args, read_config, build_permission_flags, build_prompt, build_claude_command, dry_run_output
       - Guard the "main" execution behind `if [ "${BASH_SOURCE[0]}" = "$0" ]; then` for testability (functions can be sourced individually)
       - Do NOT implement the execution loop yet (that is Plan 02)

    build_prompt must handle these GSD command translations:
    - "execute-phase N" -> "You are executing Phase N of the GSD project plan. Read STATE.md for your current position. Follow the plan instructions for the active phase. Complete as many tasks as possible within the turn limit."
    - "verify-work N" -> "You are verifying the work completed in Phase N. Read STATE.md for context. Check all success criteria and test results."
    - "plan-phase N" -> "You are planning Phase N. Read STATE.md, the phase description from ROADMAP.md, and any existing CONTEXT.md or RESEARCH.md. Create plan files following GSD conventions."
    - Default for unrecognized: "Execute the following GSD command: {raw command}. Read STATE.md for current position and follow instructions."

    build_claude_command must produce a string like:
    claude -p "PROMPT" --append-system-prompt-file "CONTEXT_FILE" --max-turns N --output-format json --worktree PERMISSION_FLAGS

    dry_run_output must:
    - Print "=== Ralph Dry Run ===" header
    - Print the full command
    - Print context file path and line count
    - Print config summary (max_turns, permission_tier, worktree: always on)
    - Print "To execute, run without --dry-run"

    CRITICAL CONSTRAINT from ARCHITECTURE.md: The script must use `env -u CLAUDECODE` before any `claude -p` invocation (Pitfall 1 from RESEARCH.md). Build this into build_claude_command by prepending `env -u CLAUDECODE`.

    Run tests -- all must PASS.
  </action>
  <verify>
    <automated>./tests/bats/bin/bats tests/ralph-permissions.bats tests/ralph-launcher.bats -x</automated>
  </verify>
  <done>
    - scripts/ralph-launcher.sh exists with all 6 core functions, individually sourceable
    - tests/ralph-permissions.bats has 4+ tests, all green
    - tests/ralph-launcher.bats has 14+ tests, all green
    - Default tier produces correct --allowedTools flag (PERM-01)
    - Auto-mode tier produces --permission-mode auto (PERM-02)
    - Yolo tier produces --dangerously-skip-permissions (PERM-03)
    - Commands always include --worktree (SAFE-01)
    - Commands always include --max-turns from config (SAFE-02)
    - --dry-run shows command without executing (AUTO-05)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /gsd:ralph command file</name>
  <files>.claude/commands/gsd/ralph.md</files>
  <action>
    Create .claude/commands/gsd/ralph.md as the GSD slash command entry point. This is a markdown prompt file (not a bash script) that Claude Code processes when the user types /gsd:ralph.

    The command file must:
    1. Have correct frontmatter: name, description, argument-hint showing usage, allowed-tools (Read, Write, Edit, Grep, Glob, Bash)
    2. Accept $ARGUMENTS which contains the GSD sub-command and any flags (e.g., "execute-phase 11 --dry-run --tier yolo")
    3. Instruct Claude to run `bash scripts/ralph-launcher.sh $ARGUMENTS` from the project root
    4. Provide brief guidance on what the launcher does (so Claude knows what to expect)
    5. Tell Claude to relay output to the user (especially dry-run output and completion/failure messages)

    The command file should be concise (~30-50 lines). It is NOT responsible for any logic -- it delegates everything to ralph-launcher.sh.

    Example structure:
    ```
    ---
    name: gsd:ralph
    description: Run a GSD command autonomously with Ralph autopilot
    argument-hint: "<gsd-command> [--dry-run] [--tier default|auto-mode|yolo]"
    allowed-tools:
      - Read
      - Write
      - Edit
      - Grep
      - Glob
      - Bash
    ---

    (objective, context with $ARGUMENTS, instructions to run launcher)
    ```

    Verify the command works by checking the file structure matches GSD command file conventions.
  </action>
  <verify>
    <automated>test -f .claude/commands/gsd/ralph.md && grep -q 'ARGUMENTS' .claude/commands/gsd/ralph.md && grep -q 'ralph-launcher' .claude/commands/gsd/ralph.md && echo "PASS"</automated>
  </verify>
  <done>
    - .claude/commands/gsd/ralph.md exists with correct frontmatter
    - File references $ARGUMENTS for GSD command passthrough
    - File instructs Claude to run scripts/ralph-launcher.sh
    - User can type /gsd:ralph execute-phase 11 and the command triggers (AUTO-01)
  </done>
</task>

</tasks>

<verification>
- All permission tier tests pass: `./tests/bats/bin/bats tests/ralph-permissions.bats`
- All launcher tests pass: `./tests/bats/bin/bats tests/ralph-launcher.bats`
- /gsd:ralph command file exists with correct structure
- ralph-launcher.sh is sourceable (functions work independently)
- Dry-run output shows complete command preview
</verification>

<success_criteria>
- scripts/ralph-launcher.sh has all core functions: parse_args, read_config, build_permission_flags, build_prompt, build_claude_command, dry_run_output
- All 3 permission tiers correctly mapped to Claude Code CLI flags
- --worktree always included, --max-turns reads from config
- --dry-run shows exact command without executing
- .claude/commands/gsd/ralph.md provides /gsd:ralph entry point
- 18+ tests green across 2 test files
</success_criteria>

<output>
After completion, create `.planning/phases/11-shell-launcher-and-headless-invocation/11-01-SUMMARY.md`
</output>
