---
phase: 21-correctness-checks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/challenges/reference-solutions/fix-bug/src/commands/done.sh
  - benchmarks/challenges/reference-solutions/fix-bug/tests/test_done.bats
  - benchmarks/challenges/reference-solutions/add-feature/src/commands/delete.sh
  - benchmarks/challenges/reference-solutions/add-feature/src/taskctl.sh
  - benchmarks/challenges/reference-solutions/add-feature/tests/test_delete.bats
  - benchmarks/challenges/reference-solutions/add-tests/tests/test_storage.bats
  - benchmarks/challenges/reference-solutions/refactor/src/format.sh
  - benchmarks/challenges/reference-solutions/multi-file/src/commands/add.sh
  - benchmarks/challenges/reference-solutions/multi-file/src/commands/list.sh
  - benchmarks/challenges/reference-solutions/multi-file/src/storage.sh
  - benchmarks/challenges/reference-solutions/multi-file/tests/test_priority.bats
  - benchmarks/challenges/checks/check-fix-bug.sh
  - benchmarks/challenges/checks/check-add-feature.sh
  - benchmarks/challenges/checks/check-add-tests.sh
autonomous: true
requirements: [CHAL-06, HARN-03]

must_haves:
  truths:
    - "Each reference solution overlay transforms baseline into a passing state for its challenge"
    - "Check scripts 1-3 FAIL against bench/baseline (negative control)"
    - "Check scripts 1-3 PASS when reference solution is overlaid onto baseline (positive control)"
  artifacts:
    - path: "benchmarks/challenges/reference-solutions/fix-bug/"
      provides: "Fixed done.sh that looks up by .id field + test_done.bats"
    - path: "benchmarks/challenges/reference-solutions/add-feature/"
      provides: "delete.sh command + updated taskctl.sh dispatch + test_delete.bats"
    - path: "benchmarks/challenges/reference-solutions/add-tests/"
      provides: "test_storage.bats with 5+ tests covering storage functions"
    - path: "benchmarks/challenges/reference-solutions/refactor/"
      provides: "Cleaned format.sh with fewer ShellCheck warnings"
    - path: "benchmarks/challenges/reference-solutions/multi-file/"
      provides: "Priority feature across add.sh, list.sh, storage.sh + test_priority.bats"
    - path: "benchmarks/challenges/checks/check-fix-bug.sh"
      provides: "Behavioral check for Challenge 1"
      min_lines: 30
    - path: "benchmarks/challenges/checks/check-add-feature.sh"
      provides: "Behavioral check for Challenge 2"
      min_lines: 30
    - path: "benchmarks/challenges/checks/check-add-tests.sh"
      provides: "Behavioral check for Challenge 3"
      min_lines: 25
  key_links:
    - from: "benchmarks/challenges/checks/check-fix-bug.sh"
      to: "benchmarks/taskctl/src/commands/done.sh"
      via: "Sources done.sh and runs cmd_done, checks jq output"
      pattern: "cmd_done.*jq.*select.*\\.id"
    - from: "benchmarks/challenges/checks/check-add-feature.sh"
      to: "benchmarks/taskctl/src/taskctl.sh"
      via: "Runs taskctl.sh delete command, verifies removal from JSON"
      pattern: "taskctl\\.sh delete"
    - from: "benchmarks/challenges/checks/check-add-tests.sh"
      to: "benchmarks/taskctl/tests/test_storage.bats"
      via: "Checks test file existence, test count, and Bats pass"
      pattern: "test_storage\\.bats"
---

<objective>
Create all 5 reference solution overlays and correctness check scripts for Challenges 1-3 (fix-bug, add-feature, add-tests), with dual-control validation proving each check reliably distinguishes passing from failing solutions.

Purpose: Reference solutions serve as positive controls for check validation. Check scripts are the evaluation logic that bench-eval.sh will invoke. Getting checks 1-3 done here enables Plan 02 to focus on checks 4-5, the bench/after-delete tag, JSON definitions, and the eval driver.

Output: 5 reference solution directories, 3 check scripts, all validated against both controls.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-correctness-checks/21-RESEARCH.md

@benchmarks/taskctl/src/taskctl.sh
@benchmarks/taskctl/src/storage.sh
@benchmarks/taskctl/src/format.sh
@benchmarks/taskctl/src/commands/add.sh
@benchmarks/taskctl/src/commands/list.sh
@benchmarks/taskctl/src/commands/done.sh
@benchmarks/taskctl/.taskctl.json
@benchmarks/taskctl/tests/test_add.bats
@benchmarks/taskctl/tests/test_list.bats

<interfaces>
<!-- Existing taskctl contracts the check scripts and reference solutions must work with -->

From benchmarks/taskctl/src/storage.sh:
```bash
STORAGE_FILE="${TASKCTL_DATA:-.taskctl.json}"
storage_read_all()    # Returns JSON array of tasks (or '[]' if empty/missing)
storage_add()         # Adds task with auto-incremented ID
storage_next_id()     # Returns max(id) + 1
```

From benchmarks/taskctl/src/taskctl.sh:
```bash
# Dispatch: add | list | done (no delete at baseline)
case "${1:-}" in
    add)  shift; source "$SCRIPT_DIR/commands/add.sh"; cmd_add "$@" ;;
    list) shift; source "$SCRIPT_DIR/commands/list.sh"; cmd_list "$@" ;;
    done) shift; source "$SCRIPT_DIR/commands/done.sh"; cmd_done "$@" ;;
    *)    echo "Usage: taskctl {add|list|done} [args]"; exit 1 ;;
esac
```

From benchmarks/taskctl/src/commands/done.sh (BUGGY -- array index, not ID lookup):
```bash
cmd_done() {
    # BUG: uses jq .[$idx] (array index) instead of select(.id == $id)
    updated=$(echo "$tasks" | jq --argjson idx "$task_id" '.[$idx].done = true')
}
```

From benchmarks/taskctl/.taskctl.json (seed data):
```json
[
  {"id": 1, "description": "Buy groceries", "done": false, "created": "2026-03-01T10:00:00Z"},
  {"id": 2, "description": "Write README", "done": true, "created": "2026-03-01T11:00:00Z"},
  {"id": 3, "description": "Fix tests", "done": false, "created": "2026-03-02T09:00:00Z"},
  {"id": 4, "description": "Deploy v2", "done": false, "created": "2026-03-02T14:00:00Z"}
]
```

Bats test pattern (from test_add.bats):
```bash
setup() {
    TEST_DIR="$(mktemp -d)"
    export TASKCTL_DATA="$TEST_DIR/.taskctl.json"
    SCRIPT_DIR="$(cd "$BATS_TEST_DIRNAME/../src" && pwd)"
    source "$SCRIPT_DIR/storage.sh"
    source "$SCRIPT_DIR/format.sh"
    source "$SCRIPT_DIR/commands/add.sh"
    load '../../../tests/test_helper/bats-support/load'
    load '../../../tests/test_helper/bats-assert/load'
}
teardown() { rm -rf "$TEST_DIR"; }
```

Bats binary path from taskctl dir: `../../tests/bats/bin/bats`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reference solution overlays for all 5 challenges</name>
  <files>
    benchmarks/challenges/reference-solutions/fix-bug/src/commands/done.sh
    benchmarks/challenges/reference-solutions/fix-bug/tests/test_done.bats
    benchmarks/challenges/reference-solutions/add-feature/src/commands/delete.sh
    benchmarks/challenges/reference-solutions/add-feature/src/taskctl.sh
    benchmarks/challenges/reference-solutions/add-feature/tests/test_delete.bats
    benchmarks/challenges/reference-solutions/add-tests/tests/test_storage.bats
    benchmarks/challenges/reference-solutions/refactor/src/format.sh
    benchmarks/challenges/reference-solutions/multi-file/src/commands/add.sh
    benchmarks/challenges/reference-solutions/multi-file/src/commands/list.sh
    benchmarks/challenges/reference-solutions/multi-file/src/storage.sh
    benchmarks/challenges/reference-solutions/multi-file/tests/test_priority.bats
  </files>
  <action>
Create 5 reference solution overlay directories under `benchmarks/challenges/reference-solutions/`. Each directory contains ONLY the files that differ from bench/baseline. When overlaid (copied) onto the baseline, the result should pass the corresponding correctness check.

**Challenge 1 (fix-bug/):**
- `src/commands/done.sh` -- Fixed version that looks up by `.id` field using `jq --argjson id "$task_id" 'map(if .id == $id then .done = true else . end)'` instead of array index. Include error handling for missing ID.
- `tests/test_done.bats` -- At least 3 tests: done marks correct task, done on nonexistent ID errors, existing tests pattern. Use same setup/teardown as test_add.bats (mktemp, TASKCTL_DATA, source done.sh). Must also source storage.sh and format.sh. Load bats-support and bats-assert with same relative path as existing tests.

**Challenge 2 (add-feature/):**
- `src/commands/delete.sh` -- New file. `cmd_delete` function: validates ID required, reads tasks via `storage_read_all`, checks task exists with jq select, filters it out with `jq '[.[] | select(.id != $id)]'`, writes back, prints confirmation. Errors if ID not found.
- `src/taskctl.sh` -- Copy of baseline taskctl.sh with `delete)` case added: `delete) shift; source "$SCRIPT_DIR/commands/delete.sh"; cmd_delete "$@" ;;`. Update usage string to include `delete`.
- `tests/test_delete.bats` -- At least 3 tests: delete removes task, delete nonexistent ID errors, delete preserves other tasks. Same setup pattern (must source delete.sh, storage.sh, format.sh).

**Challenge 3 (add-tests/):**
- `tests/test_storage.bats` -- At least 5 tests covering storage_read_all (empty file returns []), storage_add (creates task with correct fields), storage_next_id (returns max+1), storage_add multiple (IDs increment), storage_read_all after add (returns added items). Source storage.sh directly. Use TASKCTL_DATA for isolation.

**Challenge 4 (refactor/):**
- `src/format.sh` -- Cleaned version: double-quote all variables (`"$tasks"`, `"$total"`, `"$i"`), use `local` for all variables, remove redundant `done_cnt` variable, consolidate the duplicated `printf` pattern into a helper function, use `[[ ]]` instead of `[ ]`. The refactored version must produce identical output for all filter modes (all, done, pending). Target: fewer than 12 ShellCheck warnings (down from 22), more than 10 lines changed vs baseline.

**Challenge 5 (multi-file/):**
- `src/commands/add.sh` -- Modified to accept `--priority <level>` flag before the description. Parse args: if `$1` is `--priority`, shift and capture priority (validate: low/medium/high), else default to "low". Call a new `storage_add_with_priority "$description" "$priority"` function (or modify storage_add call to include priority).
- `src/commands/list.sh` -- Modified to accept `--sort priority` flag. When sorting, use jq to order tasks by priority (map high=1, medium=2, low=3). Default behavior unchanged.
- `src/storage.sh` -- Modified `storage_add` to accept optional second argument for priority. Store `"priority": "$priority"` field in the JSON object (default "low" if not provided). Keep backward compatibility.
- `tests/test_priority.bats` -- At least 4 tests: add with priority stores it, add without priority defaults to low, list --sort priority orders correctly, existing add/list behavior unchanged. Source add.sh, list.sh, storage.sh, format.sh.

**IMPORTANT:** All Bats test files must use the same `setup()`/`teardown()` isolation pattern as existing tests (mktemp, TASKCTL_DATA, source from BATS_TEST_DIRNAME, load helpers). All reference solutions must be Bash 3.2 compatible. Reference solutions are overlays -- they contain ONLY files that differ from baseline.
  </action>
  <verify>
    <automated>
# Verify all reference solution files exist
for dir in fix-bug add-feature add-tests refactor multi-file; do
  ls benchmarks/challenges/reference-solutions/$dir/ >/dev/null 2>&1 || { echo "FAIL: $dir missing"; exit 1; }
done
echo "All 5 reference solution directories exist"
    </automated>
  </verify>
  <done>
    All 5 reference solution overlay directories contain the correct files. fix-bug has done.sh + test_done.bats, add-feature has delete.sh + taskctl.sh + test_delete.bats, add-tests has test_storage.bats, refactor has format.sh, multi-file has add.sh + list.sh + storage.sh + test_priority.bats.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create check scripts for Challenges 1-3 with dual-control validation</name>
  <files>
    benchmarks/challenges/checks/check-fix-bug.sh
    benchmarks/challenges/checks/check-add-feature.sh
    benchmarks/challenges/checks/check-add-tests.sh
  </files>
  <action>
Create 3 behavioral check scripts under `benchmarks/challenges/checks/`. Each script follows this contract:
- Accepts `TASKCTL_DIR` as first argument (default: current directory)
- Uses a `check()` helper function that takes a name and a test command, increments pass/fail counters
- Outputs `PASS: name` or `FAIL: name` for each check
- Outputs `Score: N/M checks passed` at the end
- Returns exit code 0 if all pass, 1 if any fail
- All behavioral: tests outcomes, NOT code structure (per HARN-03)
- Each check that modifies data MUST use a temp copy of .taskctl.json (mktemp + trap cleanup)

**check-fix-bug.sh (3 checks):**
1. "done 3 marks task with id=3" -- Copy .taskctl.json to tmpfile, set TASKCTL_DATA to tmpfile, source storage.sh + format.sh + done.sh in a subshell, run `cmd_done 3`, verify `jq '.[] | select(.id == 3) | .done' "$tmpfile"` equals "true". At baseline this FAILS because done 3 sets array[3].done=true (task id=4).
2. "existing tests pass" -- Run `$BATS_BIN tests/test_add.bats tests/test_list.bats` from TASKCTL_DIR. The Bats binary path should be resolved relative to the script: check the existence of `$TASKCTL_DIR/../../tests/bats/bin/bats`, then try system `bats`, then fail with a message.
3. "test coverage for done command exists" -- Check that EITHER `tests/test_done.bats` exists OR the total `@test` count across all .bats files exceeds the baseline count of 7. At baseline this FAILS (no test_done.bats, exactly 7 tests).

**check-add-feature.sh (4 checks):**
1. "delete removes a task" -- Copy .taskctl.json to tmpfile, run `bash "$TASKCTL_DIR/src/taskctl.sh" delete 1` with TASKCTL_DATA pointing to tmpfile, verify task id=1 no longer in JSON. At baseline this FAILS (no delete command).
2. "delete nonexistent ID shows error" -- Run `bash "$TASKCTL_DIR/src/taskctl.sh" delete 999 2>&1` and grep for error-like text (case-insensitive: "not found|error|no task|invalid"). At baseline this FAILS.
3. "test_delete.bats exists with 2+ tests" -- Check file exists and `grep -c '@test' test_delete.bats >= 2`. At baseline this FAILS.
4. "existing tests pass" -- Run Bats on test_add.bats and test_list.bats. Should pass at both baseline and after fix.

**check-add-tests.sh (4 checks):**
1. "test_storage.bats exists" -- Check file exists. At baseline FAILS (file does not exist).
2. "at least 5 tests in test_storage.bats" -- `grep -c '@test' tests/test_storage.bats >= 5`. At baseline FAILS.
3. "all storage tests pass" -- Run `$BATS_BIN tests/test_storage.bats`. At baseline FAILS.
4. "storage.sh unchanged from baseline" -- Compare current storage.sh against `git show bench/baseline:benchmarks/taskctl/src/storage.sh` using diff. At baseline PASSES (but combined with checks 1-3 failing, overall check fails). If git is not available in the worktree, skip this check with PASS (the harness runs in a git context).

After creating each check script, validate:
- **Negative control:** Run the check against `benchmarks/taskctl/` (which is at bench/baseline state). It MUST return exit code 1 (FAIL).
- **Positive control:** Create a temp directory, copy benchmarks/taskctl/ into it, overlay the corresponding reference solution files, run the check against the temp dir. It MUST return exit code 0 (PASS). Clean up temp dir after.

Mark all 3 scripts as executable (`chmod +x`).
  </action>
  <verify>
    <automated>
# Negative control: all 3 checks FAIL against baseline
fail_count=0
for check in fix-bug add-feature add-tests; do
  if bash benchmarks/challenges/checks/check-$check.sh benchmarks/taskctl 2>/dev/null; then
    echo "UNEXPECTED PASS on baseline: $check"
  else
    fail_count=$((fail_count + 1))
    echo "OK: check-$check.sh correctly FAILS on baseline"
  fi
done
echo ""
# Positive control: all 3 checks PASS with reference solutions overlaid
pass_count=0
for check in fix-bug add-feature add-tests; do
  tmpdir=$(mktemp -d)
  cp -R benchmarks/taskctl/* "$tmpdir/" 2>/dev/null
  cp -R benchmarks/taskctl/.taskctl.json "$tmpdir/" 2>/dev/null
  cp -R benchmarks/challenges/reference-solutions/$check/* "$tmpdir/" 2>/dev/null
  if bash benchmarks/challenges/checks/check-$check.sh "$tmpdir" 2>/dev/null; then
    pass_count=$((pass_count + 1))
    echo "OK: check-$check.sh correctly PASSES with reference solution"
  else
    echo "UNEXPECTED FAIL with reference: $check"
  fi
  rm -rf "$tmpdir"
done
echo ""
echo "Negative control: $fail_count/3 correctly FAIL"
echo "Positive control: $pass_count/3 correctly PASS"
[[ "$fail_count" -eq 3 ]] && [[ "$pass_count" -eq 3 ]]
    </automated>
  </verify>
  <done>
    All 3 check scripts exist and are executable. Each FAILS against bench/baseline (negative control) and PASSES when its reference solution is overlaid (positive control). Output follows the "PASS/FAIL + Score" format.
  </done>
</task>

</tasks>

<verification>
1. All 5 reference solution directories exist with correct files
2. Check scripts 1-3 all FAIL on baseline (3/3 negative control)
3. Check scripts 1-3 all PASS with reference overlays (3/3 positive control)
4. Reference solution files are Bash 3.2 compatible
5. All Bats test files in reference solutions follow the established isolation pattern
</verification>

<success_criteria>
- 5 reference solution overlay directories exist under benchmarks/challenges/reference-solutions/
- 3 check scripts exist under benchmarks/challenges/checks/ and are executable
- Negative control: all 3 checks return exit 1 against benchmarks/taskctl/ (baseline state)
- Positive control: all 3 checks return exit 0 against baseline + reference overlay
- Check output uses "PASS: name" / "FAIL: name" / "Score: N/M" format
</success_criteria>

<output>
After completion, create `.planning/phases/21-correctness-checks/21-01-SUMMARY.md`
</output>
