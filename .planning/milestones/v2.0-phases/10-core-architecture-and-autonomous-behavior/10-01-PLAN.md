---
phase: 10-core-architecture-and-autonomous-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/gsd-ralph-autopilot/SKILL.md
  - .planning/config.json
  - scripts/validate-config.sh
  - tests/skill-validation.bats
  - tests/ralph-config.bats
  - tests/test_helper/ralph-helpers.bash
autonomous: true
requirements:
  - AUTO-04

must_haves:
  truths:
    - "SKILL.md exists in .claude/skills/gsd-ralph-autopilot/ and Claude Code can auto-discover it"
    - "SKILL.md contains rules for: never calling AskUserQuestion, always picking first option, auto-approving checkpoints with rationale + git commit, skipping human-action steps with logging"
    - "SKILL.md has user-invocable: false frontmatter so it loads as background knowledge"
    - "config.json contains a ralph key with exactly 3 fields: enabled (bool), max_turns (int), permission_tier (enum)"
    - "Config validation warns on unknown keys and invalid values without breaking"
  artifacts:
    - path: ".claude/skills/gsd-ralph-autopilot/SKILL.md"
      provides: "Autonomous behavior rules for headless execution"
      contains: "AskUserQuestion"
    - path: ".planning/config.json"
      provides: "Ralph config schema under ralph key"
      contains: "ralph"
    - path: "scripts/validate-config.sh"
      provides: "Config validation with strict-with-warnings semantics"
      exports: ["validate_ralph_config"]
    - path: "tests/skill-validation.bats"
      provides: "Tests for SKILL.md content and structure"
      min_lines: 30
    - path: "tests/ralph-config.bats"
      provides: "Tests for config schema validation"
      min_lines: 40
    - path: "tests/test_helper/ralph-helpers.bash"
      provides: "Shared test fixtures for mock STATE.md, config.json"
      min_lines: 15
  key_links:
    - from: "tests/skill-validation.bats"
      to: ".claude/skills/gsd-ralph-autopilot/SKILL.md"
      via: "file existence and content checks"
      pattern: "SKILL.md"
    - from: "tests/ralph-config.bats"
      to: "scripts/validate-config.sh"
      via: "source and function call"
      pattern: "validate_ralph_config"
---

<objective>
Create the autonomous behavior SKILL.md and Ralph config schema extension with validation.

Purpose: AUTO-04 requires a behavior prompt that prevents AskUserQuestion and auto-approves checkpoints. The config schema provides essential settings (enabled, max_turns, permission_tier) that Phase 11's launcher will read. Both are foundational artifacts referenced by all downstream phases.

Output: SKILL.md in `.claude/skills/gsd-ralph-autopilot/`, extended `config.json`, config validation script, and tests for both.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-architecture-and-autonomous-behavior/10-RESEARCH.md
@.planning/phases/10-core-architecture-and-autonomous-behavior/10-CONTEXT.md

<interfaces>
<!-- Existing test infrastructure the executor needs -->

From tests/test_helper/common.bash:
```bash
_common_setup()    # Sets PROJECT_ROOT, PATH, loads bats libs, creates TEST_TEMP_DIR
_common_teardown() # Cleans up TEST_TEMP_DIR
create_test_repo() # git init + initial commit
create_gsd_structure() # Creates .planning/ with ROADMAP.md and STATE.md
```

From .planning/config.json (current structure):
```json
{
  "mode": "yolo",
  "parallelization": true,
  "commit_docs": true,
  "model_profile": "quality",
  "workflow": { "research": true, "plan_check": true, "verifier": true },
  "granularity": "standard"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test scaffolds, SKILL.md, and config extension</name>
  <files>
    tests/test_helper/ralph-helpers.bash,
    tests/skill-validation.bats,
    tests/ralph-config.bats,
    .claude/skills/gsd-ralph-autopilot/SKILL.md,
    .planning/config.json,
    scripts/validate-config.sh
  </files>
  <behavior>
    SKILL.md validation tests (tests/skill-validation.bats):
    - Test: SKILL.md file exists at .claude/skills/gsd-ralph-autopilot/SKILL.md
    - Test: SKILL.md frontmatter contains name: gsd-ralph-autopilot
    - Test: SKILL.md frontmatter contains user-invocable: false
    - Test: SKILL.md body contains "AskUserQuestion" rule (never call it)
    - Test: SKILL.md body contains "first option" decision strategy
    - Test: SKILL.md body contains checkpoint auto-approve with git commit
    - Test: SKILL.md body contains skip human-action steps rule
    - Test: SKILL.md body contains clean exit rule (no invented work)
    - Test: SKILL.md does NOT reference specific GSD file formats or phase structures (anti-pattern from research)

    Config validation tests (tests/ralph-config.bats):
    - Test: validate_ralph_config accepts valid config with all 3 fields
    - Test: validate_ralph_config warns on non-boolean enabled value
    - Test: validate_ralph_config warns on non-integer max_turns
    - Test: validate_ralph_config warns on invalid permission_tier value
    - Test: validate_ralph_config warns on unknown keys (strict with warnings)
    - Test: validate_ralph_config succeeds when ralph key is missing (not an error)
    - Test: config.json has ralph key with enabled, max_turns, permission_tier fields
    - Test: ralph.max_turns defaults to 50
    - Test: ralph.permission_tier is one of: default, auto-mode, yolo
  </behavior>
  <action>
    **Step 1: Create shared test helpers** (tests/test_helper/ralph-helpers.bash)
    - Helper function `create_ralph_config` that writes a mock config.json with customizable ralph key fields to TEST_TEMP_DIR
    - Helper function `create_mock_state` that writes a mock STATE.md to TEST_TEMP_DIR/.planning/
    - Helper to get the real PROJECT_ROOT for checking actual project files (not test temp dir)

    **Step 2: Write failing tests** for SKILL.md (tests/skill-validation.bats)
    - Load common.bash and ralph-helpers.bash
    - Tests check the REAL project file at PROJECT_ROOT/.claude/skills/gsd-ralph-autopilot/SKILL.md
    - Use grep/assert patterns to verify content requirements from the behavior list above
    - Run tests, confirm they fail (RED)

    **Step 3: Create SKILL.md** (.claude/skills/gsd-ralph-autopilot/SKILL.md)
    - Use the complete SKILL.md from 10-RESEARCH.md Code Examples section as the starting point
    - Frontmatter: name: gsd-ralph-autopilot, description (multi-line about autonomous behavior), user-invocable: false
    - Rule 1: Never Ask Questions -- NEVER use AskUserQuestion, always pick FIRST option when GSD presents multi-option decisions, exit cleanly if genuinely stuck
    - Rule 2: Auto-Approve Checkpoints -- auto-approve human-verify checkpoints, write brief rationale WHY it passes, create git commit at each checkpoint with format `chore(checkpoint): <description>`
    - Rule 3: Skip Human-Action Steps -- skip entirely, log with full action description, mark as "SKIPPED (autonomous mode)"
    - Rule 4: Follow GSD Conventions -- read STATE.md, follow plan instructions, one task = one commit, conventional commit format, never modify PLAN.md, update STATE.md after completing tasks
    - Rule 5: Clean Exit -- exit when scope complete, do not invent additional work, do not refactor outside current task
    - IMPORTANT: Do NOT reference specific GSD file formats or phase structures (the SKILL.md contains behavior rules only, not workflow instructions)
    - Run skill tests, confirm they pass (GREEN)

    **Step 4: Write failing tests** for config validation (tests/ralph-config.bats)
    - Load common.bash and ralph-helpers.bash
    - Source scripts/validate-config.sh to get validate_ralph_config function
    - Tests use create_ralph_config helper with various valid/invalid configs
    - Run tests, confirm they fail (RED)

    **Step 5: Extend config.json** (.planning/config.json)
    - Read current config.json, add "ralph" key with: enabled: true, max_turns: 50, permission_tier: "default"
    - Use jq to add the key cleanly (preserve existing fields)

    **Step 6: Create validate-config.sh** (scripts/validate-config.sh)
    - Bash 3.2 compatible (no associative arrays, no ${var,,})
    - Function `validate_ralph_config` that takes config_file path argument
    - Check ralph key exists (if missing, warn and return 0 -- not an error)
    - Validate enabled is true/false, max_turns is integer, permission_tier is one of default/auto-mode/yolo
    - Warn on unknown keys under ralph (strict with warnings per user decision)
    - Use jq for all JSON parsing (do NOT hand-roll JSON parsing)
    - Make executable: chmod +x
    - Run config tests, confirm they pass (GREEN)
  </action>
  <verify>
    <automated>./tests/bats/bin/bats tests/skill-validation.bats tests/ralph-config.bats</automated>
  </verify>
  <done>
    - SKILL.md exists at .claude/skills/gsd-ralph-autopilot/SKILL.md with user-invocable: false frontmatter
    - SKILL.md contains all 5 rules (never AskUserQuestion, first option, auto-approve checkpoints with commit, skip human-action, clean exit)
    - config.json has ralph key with enabled: true, max_turns: 50, permission_tier: "default"
    - validate-config.sh validates known fields and warns on unknown keys
    - All skill-validation.bats and ralph-config.bats tests pass
  </done>
</task>

</tasks>

<verification>
- `./tests/bats/bin/bats tests/skill-validation.bats tests/ralph-config.bats` -- all tests green
- `test -f .claude/skills/gsd-ralph-autopilot/SKILL.md` -- SKILL.md exists
- `grep -q "user-invocable: false" .claude/skills/gsd-ralph-autopilot/SKILL.md` -- skill is background knowledge
- `jq -e '.ralph.max_turns == 50' .planning/config.json` -- config schema correct
- `bash scripts/validate-config.sh` sources cleanly (no syntax errors)
</verification>

<success_criteria>
- SKILL.md auto-discoverable by Claude Code (correct path and frontmatter)
- Config schema has exactly 3 fields under ralph key
- Config validation is strict-with-warnings (accepts unknown keys with warning)
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-architecture-and-autonomous-behavior/10-01-SUMMARY.md`
</output>
