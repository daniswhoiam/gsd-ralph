---
phase: 09-cli-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - tests/guidance.bats
autonomous: true
requirements:
  - GUID-01
  - GUID-02

must_haves:
  truths:
    - "A test verifies gsd-ralph init success output contains 'Next:'"
    - "A test verifies gsd-ralph init already-initialized output contains 'Next:'"
    - "A test verifies gsd-ralph execute --dry-run output contains 'Next:'"
    - "A test verifies gsd-ralph execute success output contains 'Next:'"
    - "A test verifies gsd-ralph generate success output contains 'Next:'"
    - "A test verifies gsd-ralph merge with no unmerged branches output contains 'Next:'"
    - "A test verifies gsd-ralph merge full success output contains 'Next:'"
    - "A test verifies gsd-ralph cleanup success output contains 'Next:'"
    - "A test verifies guidance is context-sensitive (different messages for different outcomes)"
  artifacts:
    - path: "tests/guidance.bats"
      provides: "Comprehensive guidance output tests for all commands"
      min_lines: 80
  key_links:
    - from: "tests/guidance.bats"
      to: "lib/common.sh"
      via: "Sources common.sh which provides print_guidance()"
      pattern: "source.*common\\.sh"
    - from: "tests/guidance.bats"
      to: "lib/commands/*.sh"
      via: "Calls cmd_* functions and checks output for Next:"
      pattern: "assert_output.*Next:"
---

<objective>
Add comprehensive test coverage verifying that all commands produce context-sensitive "Next:" guidance.

Purpose: Without test coverage, future changes could accidentally remove guidance from exit paths. Tests ensure every command's guidance survives refactoring.
Output: `tests/guidance.bats` with tests covering all major exit paths across all commands.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cli-guidance/09-RESEARCH.md
@.planning/phases/09-cli-guidance/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guidance test suite for init, execute, and generate</name>
  <files>tests/guidance.bats</files>
  <action>
Create `tests/guidance.bats` following the existing test patterns (see `tests/init.bats`, `tests/execute.bats` for reference). Use the standard test_helper/common.bash setup/teardown pattern.

**Test setup requirements:**
- Source `test_helper/common.bash`
- Use `_common_setup` and `_common_teardown` (creates temp git repo with .planning/ and .ralph/ dirs)
- Export `GSD_RALPH_HOME=$PROJECT_ROOT` before sourcing lib modules (per safety.bats pattern from Phase 7)
- Source `lib/common.sh` for output helpers
- Source the individual command files as needed per test

**Tests to write:**

1. **`print_guidance outputs formatted message`** — Unit test for the helper itself. Call `print_guidance "test message"` and assert output contains "Next:" and "test message". Do NOT check for color codes (tests may run without TTY).

2. **`init success shows guidance`** — Set up a fresh project dir (no .ralph/), run `cmd_init`, assert output contains "Next:". Assert output does NOT contain the old "Next steps:" multi-line format.

3. **`init already initialized shows guidance`** — Set up project dir with existing .ralph/, run `cmd_init` (without --force), assert output contains "Next:" and "reinitialize".

4. **`execute dry-run shows guidance`** — Set up a project with a valid phase and plan files (reuse the fixture creation pattern from tests/execute.bats), run `cmd_execute --dry-run <N>`, assert output contains "Next:" and "execute".

5. **`execute success shows guidance`** — Run `cmd_execute <N>` with valid phase setup, assert output contains "Next:" and "ralph".

6. **`generate success shows guidance`** — Set up a project with valid phase/plan files, run `cmd_generate <N>`, assert output contains "Next:" and "generated files" or the output dir path.

For fixtures, follow the exact pattern used in existing bats files:
- Create `.planning/phases/NN-test/` directory
- Create a minimal plan file matching the `[0-9][0-9]-[0-9][0-9]-PLAN.md` glob
- Include minimal valid frontmatter (phase, plan, type, wave, depends_on, files_modified, autonomous, must_haves)
- Include a minimal `<tasks><task type="auto"><name>Test task</name><action>test</action><verify>true</verify><done>done</done></task></tasks>` block

Important: Follow the GSD_RALPH_HOME export pattern from tests/safety.bats line 10 — export GSD_RALPH_HOME="$PROJECT_ROOT" before any source statements that chain-load safety.sh.
  </action>
  <verify>
    <automated>cd /Users/daniswhoiam/Projects/gsd-ralph && ./tests/bats/bin/bats tests/guidance.bats 2>&1 | tail -20</automated>
    <manual>All guidance tests for init, execute, and generate pass</manual>
  </verify>
  <done>tests/guidance.bats exists with passing tests for: print_guidance helper, init success, init already-initialized, execute dry-run, execute success, generate success.</done>
</task>

<task type="auto">
  <name>Task 2: Add guidance tests for merge and cleanup, plus context-sensitivity check</name>
  <files>tests/guidance.bats</files>
  <action>
Add the following tests to `tests/guidance.bats` (append to the file created in Task 1):

**Merge tests:**

7. **`merge no unmerged branches shows guidance`** — Set up a project where no phase branches exist, run `cmd_merge <N>`, assert output contains "Next:" and "execute". This tests the "nothing to merge" path.

8. **`merge full success shows guidance`** — Create a phase branch with a commit, merge it. Assert output contains "Next:" and "cleanup". This requires:
   - Creating a test branch (`phase-1/test-slug`), making a commit on it, switching back to main.
   - Running `cmd_merge 1` and checking for guidance.
   - Use the same bare-repo remote pattern from tests/merge.bats if push.sh is sourced.

9. **`merge dry-run shows guidance`** — Create a phase branch, run `cmd_merge <N> --dry-run`, assert output contains "Next:".

10. **`merge rollback shows guidance`** — Create a merge rollback point file, run `cmd_merge <N> --rollback`, assert output contains "Next:" and "re-run".

**Cleanup tests:**

11. **`cleanup nothing to clean shows no guidance`** — Run cleanup when no branches/registry entries exist. Assert output does NOT contain "Next:" (nothing happened, nothing to suggest).

12. **`cleanup success shows guidance`** — Register a branch in the registry, run cleanup with --force, assert output contains "Next:" and "cleaned up".

13. **`cleanup unregistered branches without force shows guidance`** — Create an unregistered phase branch, run cleanup without --force, assert output contains "Next:" and "--force".

**Context-sensitivity test:**

14. **`guidance is context-sensitive across commands`** — Run init (success) and capture its guidance message, run generate and capture its guidance message. Assert the two messages are DIFFERENT. This proves GUID-02 (context-sensitive guidance, not a generic message).

For merge/cleanup tests, follow patterns from existing `tests/merge.bats` and `tests/cleanup.bats`:
- Merge tests need `.ralphrc` with `AUTO_PUSH=false` to avoid push attempts
- Cleanup tests need the worktree registry module sourced
- Use `--force` flag for cleanup to skip interactive confirmation
- Load config.sh before merge (for load_ralphrc)

After all tests are added, run the full test suite (`make test`) to ensure no regressions.
  </action>
  <verify>
    <automated>cd /Users/daniswhoiam/Projects/gsd-ralph && ./tests/bats/bin/bats tests/guidance.bats 2>&1 | tail -30 && make test 2>&1 | tail -5</automated>
    <manual>All guidance tests pass. Full test suite still passes. At least 12 tests in guidance.bats.</manual>
  </verify>
  <done>tests/guidance.bats contains 12+ passing tests covering all command guidance output. Tests verify both presence and context-sensitivity of guidance. Full test suite (make test) passes with no regressions.</done>
</task>

</tasks>

<verification>
1. `./tests/bats/bin/bats tests/guidance.bats` — all tests pass
2. `make test` — full suite passes, no regressions
3. Test count in guidance.bats is >= 12 (covering all major exit paths)
4. At least one test verifies context-sensitivity (different messages for different outcomes)
</verification>

<success_criteria>
All guidance tests pass. Full test suite passes. Every command's primary exit paths have test coverage verifying "Next:" output. Context-sensitivity is explicitly tested (different guidance for different outcomes).
</success_criteria>

<output>
After completion, create `.planning/phases/09-cli-guidance/09-02-SUMMARY.md`
</output>
