---
phase: 09-cli-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/common.sh
  - lib/commands/init.sh
  - lib/commands/execute.sh
  - lib/commands/generate.sh
  - lib/commands/merge.sh
  - lib/commands/cleanup.sh
  - lib/merge/rollback.sh
autonomous: true
requirements:
  - GUID-01
  - GUID-02

must_haves:
  truths:
    - "After gsd-ralph init succeeds, output contains a Next: line suggesting the next command"
    - "After gsd-ralph init finds existing .ralph/, output contains a Next: line about reinitializing"
    - "After gsd-ralph execute N completes (success or dry-run), output contains a context-appropriate Next: line"
    - "After gsd-ralph generate N completes, output contains a Next: line about reviewing generated files"
    - "After gsd-ralph merge N completes (any outcome), output contains a context-appropriate Next: line"
    - "After gsd-ralph cleanup N completes (any outcome), output contains a context-appropriate Next: line"
    - "After gsd-ralph merge N --rollback succeeds, output contains a Next: line"
    - "No guidance appears after die() calls or --help output"
  artifacts:
    - path: "lib/common.sh"
      provides: "print_guidance() helper function"
      contains: "print_guidance"
    - path: "lib/commands/init.sh"
      provides: "Guidance at init exit points"
      contains: "print_guidance"
    - path: "lib/commands/execute.sh"
      provides: "Guidance at execute exit points"
      contains: "print_guidance"
    - path: "lib/commands/generate.sh"
      provides: "Guidance at generate exit point"
      contains: "print_guidance"
    - path: "lib/commands/merge.sh"
      provides: "Guidance at all merge exit points"
      contains: "print_guidance"
    - path: "lib/commands/cleanup.sh"
      provides: "Guidance at cleanup exit points"
      contains: "print_guidance"
    - path: "lib/merge/rollback.sh"
      provides: "Guidance after rollback success"
      contains: "print_guidance"
  key_links:
    - from: "lib/common.sh"
      to: "all command files"
      via: "print_guidance() function sourced through common.sh"
      pattern: "print_guidance"
    - from: "lib/commands/merge.sh"
      to: "lib/merge/rollback.sh"
      via: "rollback_merge() called from cmd_merge when --rollback flag set"
      pattern: "rollback_merge.*phase_num"
---

<objective>
Add context-sensitive "what to do next" guidance to every gsd-ralph command exit point.

Purpose: New users hitting gsd-ralph for the first time should never be left wondering "what now?" after a command completes. Every normal completion path should show a clear next step.
Output: A `print_guidance()` helper in `lib/common.sh` and guidance calls at every command exit point across all 5 active commands plus rollback.
</objective>

<execution_context>
@/Users/daniswhoiam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniswhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cli-guidance/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add print_guidance() helper and wire into init, execute, generate</name>
  <files>lib/common.sh, lib/commands/init.sh, lib/commands/execute.sh, lib/commands/generate.sh</files>
  <action>
1. **Add `print_guidance()` to `lib/common.sh`** — Insert after the `ring_bell()` function (around line 51). The function takes one argument (the guidance message) and prints it with a green "Next:" prefix on a new line:
   ```bash
   # Print a next-step guidance message after command completion.
   # Visually distinct from status output — helps user see what to do next.
   # Args: message (the guidance text)
   # Do NOT call after die() — the error message is the guidance for fatal errors.
   print_guidance() {
       printf "\n${GREEN}  Next:${NC} %s\n" "$1"
   }
   ```

2. **Wire into `lib/commands/init.sh`** — Replace the existing 4-line "Next steps:" block (lines 100-103: `print_info "Next steps:"` + 3 printf lines) with a single `print_guidance` call:
   - At success (end of function, after line 99 "Created .ralphrc"): `print_guidance "Review .ralphrc, then run: gsd-ralph execute <phase>"`
   - At already-initialized exit (line 47, after print_warning about .ralph/ existing, before `exit 0`): `print_guidance "To reinitialize: gsd-ralph init --force"`

3. **Wire into `lib/commands/execute.sh`** — Two exit paths:
   - Dry-run exit (line 156, `return 0`): Add before the return: `print_guidance "To execute for real: gsd-ralph execute $phase_num"`
   - Success exit (end of function, around line 255): Replace `print_success "Run 'ralph' to start execution"` with `print_guidance "Run 'ralph' to start autonomous execution"`. Place BEFORE `ring_bell` and `trap - EXIT`.

4. **Wire into `lib/commands/generate.sh`** — One exit path:
   - Success (end of function, after the summary block around line 157): Add: `print_guidance "Review generated files in $output_dir"`
  </action>
  <verify>
    <automated>cd /Users/daniswhoiam/Projects/gsd-ralph && grep -c 'print_guidance' lib/common.sh lib/commands/init.sh lib/commands/execute.sh lib/commands/generate.sh</automated>
    <manual>Verify print_guidance function exists in common.sh, init.sh has 2 calls (success + already-initialized), execute.sh has 2 calls (dry-run + success), generate.sh has 1 call</manual>
  </verify>
  <done>print_guidance() function exists in lib/common.sh. init.sh has guidance at both success and already-initialized exits (old 4-line block removed). execute.sh has guidance at dry-run and success exits. generate.sh has guidance at success exit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire guidance into merge, cleanup, and rollback</name>
  <files>lib/commands/merge.sh, lib/commands/cleanup.sh, lib/merge/rollback.sh</files>
  <action>
1. **Wire into `lib/commands/merge.sh`** — Multiple exit paths. Add `print_guidance()` at each:
   - **No unmerged branches** (around line 238, after `print_info "No unmerged branches found..."`, before `_restore_merge_stash`): `print_guidance "Run: gsd-ralph execute $phase_num  (if not yet executed)"`
   - **Dry-run clean** (around line 298, before `return 0` in dry_run block): `print_guidance "Ready to merge: gsd-ralph merge $phase_num"`
   - **Dry-run conflicts** (around line 296, before `return 1` in dry_run block): `print_guidance "Resolve conflicts before merging"`
   - **All branches conflict** (around line 310, after conflict guidance loop, before `_restore_merge_stash`): `print_guidance "Resolve conflicts manually, then re-run: gsd-ralph merge $phase_num"`
   - **Test regressions** (around line 388, after the "regressions detected" error): `print_guidance "To undo: gsd-ralph merge $phase_num --rollback"`
   - **Full success** (inside the Wave Signaling block, around line 422 in the `if [[ $skip_count -eq 0 ]] && [[ $conflict_branch_count -eq 0 ]]` branch): `print_guidance "Run: gsd-ralph cleanup $phase_num"`
   - **Partial success** (in the else branch around line 424, after the "not yet complete" info): `print_guidance "Resolve remaining conflicts, then re-run: gsd-ralph merge $phase_num"`

   Important: The final block (lines 473-479) has `ring_bell` on both failure and success return paths. Place the full-success and partial-success guidance calls in the Wave Signaling section (Phase 5) rather than at the very end, because the Wave Signaling section already differentiates between full/partial success. The test-regression guidance goes right after the error message in Phase 4. The ring_bell at the end still fires after guidance.

2. **Wire into `lib/merge/rollback.sh`** — One exit path:
   - After successful rollback (after `print_success "Rolled back to pre-merge state..."` and after the stash restoration block, at the end of the function): `print_guidance "Fix the issue, then re-run: gsd-ralph merge $phase_num"`

3. **Wire into `lib/commands/cleanup.sh`** — Multiple exit paths:
   - **Nothing to clean** (line 136, `print_info "Nothing to clean..."` before `return 0`): No guidance — nothing happened, no action needed.
   - **Unregistered branches found, no --force** (around line 131-132, after `print_info "Use --force to clean these up."`): `print_guidance "Use --force to clean: gsd-ralph cleanup $phase_num --force"`
   - **Unregistered branches found, --force done** (around line 128-129, after "Deleted N unregistered branch(es)"): `print_guidance "Phase $phase_num fully cleaned up"`
   - **User aborted confirmation** (around line 159-160, after `print_info "Aborted."`): No guidance — user chose to stop.
   - **Normal cleanup complete** (end of function, around line 263, after `print_success "Phase $phase_num cleanup complete"`): `print_guidance "Phase $phase_num fully cleaned up"`
   - **Some branches skipped (unmerged)**: Check if br_skipped > 0 in the summary section (around line 262). If skipped branches exist, use: `print_guidance "Merge or force-delete remaining branches: gsd-ralph cleanup $phase_num --force"` instead of the default. Add this conditional check after the summary block and before the final return.
  </action>
  <verify>
    <automated>cd /Users/daniswhoiam/Projects/gsd-ralph && grep -c 'print_guidance' lib/commands/merge.sh lib/commands/cleanup.sh lib/merge/rollback.sh</automated>
    <manual>Verify merge.sh has 7 print_guidance calls (no-branches, dry-run-clean, dry-run-conflict, all-conflict, test-regression, full-success, partial-success). cleanup.sh has 3-4 calls (unregistered-no-force, unregistered-force, normal-complete, skipped-branches). rollback.sh has 1 call.</manual>
  </verify>
  <done>merge.sh has context-sensitive guidance at all 7 exit paths. cleanup.sh has guidance at relevant exit paths (not at abort or nothing-to-clean). rollback.sh has guidance after successful rollback. Existing tests still pass (make test).</done>
</task>

</tasks>

<verification>
1. `make test` passes — no regressions from adding print_guidance calls
2. `grep -r 'print_guidance' lib/` shows calls in common.sh + all 5 command files + rollback.sh
3. No `print_guidance` calls appear after `die()` or in `--help` paths
4. The old init "Next steps:" 4-line block is removed (replaced by single print_guidance call)
</verification>

<success_criteria>
Every non-die, non-help exit path in init, execute, generate, merge, cleanup, and rollback has a `print_guidance()` call with context-appropriate messaging. The old init "Next steps:" block is replaced. `make test` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cli-guidance/09-01-SUMMARY.md`
</output>
